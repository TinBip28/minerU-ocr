# MinerU OCR Production - Minimal dependencies
# Target: Vietnamese text + table + seal OCR without VLM
# DO NOT install: vllm, gradio, openai, mlx

# PyTorch + Transformers (needed by PP-DocLayoutV2)
torch>=2.6.0,<3
torchvision
safetensors>=0.4.0,<1
transformers>=4.57.3,<5.0.0
accelerate>=1.5.1

# VietOCR Vietnamese text recognition
vietocr>=0.3.11,<1
six>=1.17.0,<2

# PDF / image processing
pillow>=11.0.0
numpy>=1.21.6
pypdfium2>=4.30.0,<6.0.0
pypdf>=5.6.0
pdftext>=0.6.3,<0.8.0
opencv-python>=4.11.0.86

# Model download (needed for init-models)
modelscope>=1.26.0
huggingface-hub[hf_xet]>=0.32.4
requests
filelock>=3.0.0

# Geometry / OCR utilities
shapely>=2.0.7,<3
pyclipper>=1.3.0,<2
ftfy>=6.3.1,<7

# NOTE: mineru-vl-utils is NOT required for OCR production
#       It's only needed for VLM-based inference (high/xhigh tier)
#       See: mineru/backend/hybrid/hybrid_analyze.py:_load_vlm_content_block()

# Runtime
pyyaml>=6.0.1,<7
tqdm>=4.67.1
httpx
loguru>=0.7.2
click>=8.1.7
packaging
reportlab
fast-langdetect>=0.2.3,<0.3.0
jieba>=0.42.1
onnxruntime>=1.17.0
json-repair>=0.46.2

# API server
fastapi
python-multipart
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0

# Utilities
aiosqlite>=0.20.0,<1
watchfiles>=1.0.0,<2
beautifulsoup4>=4.13.5,<5
lxml>=4.0.0,<7.0
